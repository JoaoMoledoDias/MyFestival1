<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools"
    package="pt.ipg.myfestival">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".DeletePayment"></activity>
        <activity android:name=".EditPayment" />
        <activity android:name=".NewPayment" />
        <activity android:name=".Payments" />
        <activity android:name=".DeleteEmployee" />
        <activity android:name=".EditEmployee" />
        <activity android:name=".Employee" />
        <activity android:name=".addemployees" />
        <activity android:name=".DeleteFestivais" />
        <activity android:name=".EditarFestivais" />
        <activity android:name=".adicionarFestival" />
        <activity android:name=".Festivais" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>